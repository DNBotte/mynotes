# QUICK PANDAS
#### So I don't have to search around everytime I need to use this stuff

### OneLiner Regex

df['cleanedUp'] = df['needsCleanedUp'].map(lambda x: re.sub(r'\W+', '', x))

### Drop w/o Reassignment:

df.drop('column_name', axis=1, inplace=True)
