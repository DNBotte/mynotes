# An attempt to rewrite an exam statistics analyzer for Python 2.7 written back in the teacher days
# done from memory for now, then googling later to get it right
# A version for Python 3 will also be made

# finish this at home on phone

def exmStats(vals):
    lenny = len(val)
    total = 0
    for val in vals:
        total += val
    avg = float(total/val)
    
    print "Exam results: " + "Average: " + str(avg) + " Lowest: " + str(min(vals)) + " Highest: " + str(max(vals)) + " Median: " + 
        
